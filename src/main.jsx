import React from "react";
import ReactDOM from "react-dom/client";
import RomanticWordGame from "./RomanticWordGame";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RomanticWordGame />
  </React.StrictMode>
);